# Tauri アプリケーションテンプレート 要件定義

このドキュメントは、Tauri v2 でアプリ開発を始めるためのテンプレートリポジトリに
「何を含めるか」を洗い出したものです。実装の設計図ではなく、スコープの合意を取るための一覧です。

---

## 1. 目的とスコープ

### 目的

新規 Tauri アプリを立ち上げるとき、毎回発生する以下の作業をゼロにする。

- ビルド基盤・型共有・Lint・CI の初期構築
- デスクトップ／モバイル両対応のためのプラットフォーム分岐の設計
- ロギング・設定永続化・DB・エラーハンドリングといった「どのアプリでも要る」土台
- 署名とリリース配信のパイプライン

### スコープに含むもの

- 動作するサンプル実装を伴う、そのまま開発を開始できる状態のテンプレート
- Windows / macOS / Linux / iOS / Android の 5 ターゲット
- CI（検証）と CD（リリース）のワークフロー
- テスト駆動開発を回せるテスト基盤と、それを規約で終わらせないための強制の仕組み
- 手書きの範囲を狭めるコード生成とスキャフォールド
- レビュー観点の明文化と、Copilot によるレビューの設定
- 開発者向けドキュメントと規約

### スコープに含まないもの

- 特定ドメインの業務ロジック
- 認証基盤やバックエンドサーバーの実装（連携するための足回りのみ用意する）
- ストアへの申請作業そのもの（成果物の生成までを担当）

---

## 2. 技術選定（決定事項）

| 領域 | 選定 | 補足 |
|---|---|---|
| フレームワーク | Tauri v2 | モバイル対応のため v2 必須 |
| フロントエンド | React + TypeScript (strict) | |
| ビルドツール | Vite | |
| スタイリング / UI | Tailwind CSS + shadcn/ui | コンポーネントをリポジトリ内に保持する方式 |
| Rust ↔ TS 型共有 | tauri-specta | コマンド・イベントの型付きクライアントを自動生成 |
| ルーティング | TanStack Router | パス・パラメータが型安全。IPC の型安全性と方針を揃える |
| クライアント状態 | Zustand | UI の状態のみを持つ |
| 非同期状態（Rust 呼び出し） | TanStack Query | ローディング・エラー・キャッシュ・再取得を一元化する |
| ローカル DB | sqlx + SQLite（Rust 側で完結） | フロントには command 経由でのみ公開 |
| i18n | 日本語 / 英語の 2 ロケール | OS ロケール追従。後付けは全画面の修正になるため初めから入れる |
| Rust クレート構成 | ワークスペース分割 | 純粋ロジックを tauri 非依存の別クレートに切り出す（§9.3） |
| パッケージマネージャ | pnpm | |
| Lint / Format (JS) | Biome | |
| Lint / Format (Rust) | rustfmt + clippy | |
| 対応 OS | Windows / macOS / Linux / iOS / Android | |
| ライセンス | MIT-0 | 生成先プロジェクトに著作権表示の保持義務を負わせない |

---

## 3. プラットフォーム別の対応可否

モバイルを含めるため、テンプレートは **共通コア + デスクトップ拡張 + モバイル拡張** の三層構造とする。
Rust 側は `#[cfg(desktop)]` / `#[cfg(mobile)]`、TS 側は実行時のプラットフォーム判定で分岐する。

| 機能 | Desktop | Mobile | 備考 |
|---|:--:|:--:|---|
| システムトレイ / ネイティブメニュー | ○ | — | デスクトップ専用 |
| グローバルショートカット | ○ | — | |
| ウィンドウ状態の復元 | ○ | — | |
| 自動起動 | ○ | — | |
| 単一インスタンス制御 | ○ | — | |
| 自動アップデート (updater) | ○ | — | モバイルはストア配信のため使用不可 |
| ディープリンク | ○ | ○ | 登録方法が OS ごとに異なる |
| 通知 / ダイアログ / FS / HTTP / SQL | ○ | ○ | 権限要求のフローがモバイルでは別途必要 |
| 生体認証 | — | ○ | モバイル専用 |

UI も、トレイ常駐前提のデスクトップ画面とタッチ前提のモバイル画面は別物になるため、
**レスポンシブ設計とセーフエリア対応を初期段階から組み込む**。

---

## 4. 要件一覧

優先度は以下の意味で用いる。

- **P0** — テンプレートの土台。これが無いと成立しない
- **P1** — 「実用フル装備」として初版に含める
- **P2** — 初版では見送り、レシピ集（`docs/recipes/`）として手順のみ提供する

### 4.1 プロジェクト基盤

| ID | 要件 | 優先度 |
|---|---|:--:|
| BASE-01 | ディレクトリ構成の確定（§5）とその規約ドキュメント化 | P0 |
| BASE-02 | ツールチェーンのバージョン固定（`rust-toolchain.toml`, `.node-version`, `packageManager`） | P0 |
| BASE-03 | Vite + React + TypeScript strict の初期設定、パスエイリアス（`@/`） | P0 |
| BASE-04 | 環境変数の扱いの規約（フロントは `VITE_` 接頭辞のみ、機密は Rust 側で保持）、`.env.example` | P0 |
| BASE-05 | アプリ名・識別子・アイコンを一括で差し替えるセットアップスクリプト | P1 |
| BASE-06 | `.gitignore` / `.editorconfig` / VSCode 推奨拡張・設定 | P1 |

### 4.2 フロントエンド

| ID | 要件 | 優先度 |
|---|---|:--:|
| FE-01 | Tailwind + shadcn/ui のセットアップ、デザイントークン定義 | P0 |
| FE-02 | ライト／ダーク／OS 追従のテーマ切替 | P0 |
| FE-03 | TanStack Router のセットアップと、画面追加のサンプル 2〜3 画面 | P0 |
| FE-04 | Zustand（UI 状態）と TanStack Query（Rust 呼び出し）の使い分け規約 | P0 |
| FE-05 | エラーバウンダリと、Rust 側エラーの共通表示（トースト等） | P0 |
| FE-06 | 起動時スプラッシュ／初期化待ち画面 | P1 |
| FE-07 | レスポンシブ設計（デスクトップ／モバイルのレイアウト切替）とセーフエリア対応 | P1 |
| FE-08 | i18n — 日英 2 ロケール、OS ロケール追従、言語切替 UI | P1 |
| FE-09 | アイコン・フォントのローカル同梱（オフライン動作のため CDN 依存を排除） | P1 |
| FE-10 | キーボードショートカット、フォーカス管理などのアクセシビリティ基礎 | P2 |

### 4.3 Rust 側（src-tauri）

| ID | 要件 | 優先度 |
|---|---|:--:|
| RS-01 | モバイル対応のためのエントリ構成（`lib.rs` にビルダーを置き `mobile_entry_point` を設定） | P0 |
| RS-02 | `tauri.conf.json` の初期設定とアイコン一式 | P0 |
| RS-03 | **capabilities / permissions の最小権限設計**（default / desktop / mobile の 3 セット） | P0 |
| RS-04 | コマンド設計規約と統一エラー型（`thiserror` → serde でフロントへ、型は specta で共有） | P0 |
| RS-05 | tauri-specta による TS バインディング生成と、生成物の CI 差分チェック | P0 |
| RS-06 | 共有状態（`tauri::State` + 非同期ロック）の雛形 | P0 |
| RS-07 | 構造化ロギング（ファイル出力 + ローテーション + レベルの環境変数制御） | P1 |
| RS-08 | 設定の永続化（軽量な key-value ストア。保存先は OS 標準のアプリデータディレクトリ） | P1 |
| RS-09 | SQLite + sqlx。マイグレーションの埋め込み、CI 用のオフラインクエリ検証データ | P1 |
| RS-10 | 長時間処理の雛形（非同期コマンド、進捗イベント通知、キャンセル機構） | P1 |
| RS-11 | 外部 API 連携の足回り（HTTP クライアント、リトライ、タイムアウト） | P1 |
| RS-12 | 資格情報の安全な保管（OS キーチェーン連携） | P1 |
| RS-13 | パニックハンドラと、クラッシュ時のログ退避 | P1 |

### 4.4 アプリ機能（プラグイン）

| ID | 要件 | 優先度 | 対象 |
|---|---|:--:|---|
| APP-01 | ファイル選択ダイアログ／ファイル読み書き | P0 | 両方 |
| APP-02 | ネイティブ通知 | P1 | 両方 |
| APP-03 | 外部リンクを OS 既定ブラウザで開く | P1 | 両方 |
| APP-04 | システムトレイ常駐 + トレイメニュー | P1 | Desktop |
| APP-05 | ネイティブメニューバー | P1 | Desktop |
| APP-06 | ウィンドウ位置・サイズの記憶と復元 | P1 | Desktop |
| APP-07 | 単一インスタンス制御（二重起動時は既存ウィンドウを前面化） | P1 | Desktop |
| APP-08 | 自動アップデート（署名検証込み） | P1 | Desktop |
| APP-09 | ディープリンク（カスタム URL スキーム、OAuth コールバック受け） | P1 | 両方 |
| APP-10 | グローバルショートカット | P2 | Desktop |
| APP-11 | 自動起動（ログイン時起動） | P2 | Desktop |
| APP-12 | クリップボード連携 | P2 | 両方 |
| APP-13 | 生体認証 | P2 | Mobile |

### 4.5 品質・テスト（TDD 前提）

本テンプレートは **テスト駆動開発（TDD）を前提とした開発サイクル** を標準とする。
そのためテスト基盤は「後から足すもの」ではなく P0 の土台として扱い、Phase 1 の時点で
Red-Green-Refactor が両言語で回る状態にする。進め方の詳細は §9、
規約を守らせるための強制の仕組みは §9.6 に定義する。

| ID | 要件 | 優先度 |
|---|---|:--:|
| QA-01 | Biome による lint / format（CI で違反を失敗扱い） | P0 |
| QA-02 | rustfmt + clippy（`-D warnings`） | P0 |
| QA-03 | TypeScript の型チェックを CI に組み込む | P0 |
| QA-04 | フロントのテスト基盤（Vitest + Testing Library）と、失敗するテストから書き始めるサンプル | P0 |
| QA-05 | Tauri IPC のモック層 — Rust を起動せずにコマンド呼び出しをテストできる状態（`@tauri-apps/api/mocks`） | P0 |
| QA-06 | Rust のテスト基盤 — モックアプリ経由でコマンドの入出力を検証できる雛形（`tauri::test`） | P0 |
| QA-07 | DB を含むテストの雛形（インメモリ SQLite を用い、テストごとに独立したスキーマを構築する） | P0 |
| QA-08 | ウォッチモードの整備（TS / Rust 双方）— TDD の回転速度を担保する | P0 |
| QA-09 | テストの配置・命名規約のドキュメント化（`docs/testing.md`） | P0 |
| QA-10 | カバレッジ計測（TS / Rust 両方）と CI での可視化。閾値による失敗はさせない | P1 |
| QA-11 | Git hooks（コミット前の format / lint、push 前の型チェックと高速テスト） | P1 |
| QA-12 | Conventional Commits の強制（commitlint） | P1 |
| QA-13 | テストデータのファクトリ／フィクスチャ規約 | P1 |
| QA-14 | E2E テスト（WebDriver ベース。macOS では動作しない制約を明記し、TDD サイクルの外に置く） | P2 |
| QA-15 | カバレッジ閾値を**任意で有効化できる**仕組み（既定は無効。派生プロジェクト側で有効化する） | P2 |

### 4.6 CI / CD・リリース

| ID | 要件 | 優先度 |
|---|---|:--:|
| CI-01 | PR チェック（lint / typecheck / test / clippy）と Rust・pnpm のキャッシュ | P0 |
| CI-02 | デスクトップ 3 OS のマトリクスビルド（Linux の依存パッケージ導入を含む） | P0 |
| CI-03 | タグ起因のリリースワークフロー（インストーラ生成 + GitHub Releases への添付） | P1 |
| CI-04 | 署名の組み込み — macOS: Developer ID 署名 + 公証、Windows: コード署名 | P1 |
| CI-05 | アップデータ用の署名鍵と配信マニフェストの生成 | P1 |
| CI-06 | Android のビルド（APK / AAB 生成、キーストア署名） | P1 |
| CI-07 | iOS のビルド（アーカイブ生成。証明書とプロビジョニングの取り回し） | P2 |
| CI-08 | バージョン採番と CHANGELOG の自動生成 | P1 |
| CI-09 | 依存関係の自動更新（Renovate / Dependabot） | P1 |
| CI-10 | 脆弱性スキャン（`cargo audit` / `pnpm audit`） | P1 |

### 4.7 セキュリティ

| ID | 要件 | 優先度 |
|---|---|:--:|
| SEC-01 | CSP の明示的な設定（インライン実行を許可しない） | P0 |
| SEC-02 | `withGlobalTauri` を無効化し、生成されたバインディング経由のみで呼ばせる | P0 |
| SEC-03 | capabilities は「必要な window に必要な権限だけ」を原則とする（RS-03 と対） | P0 |
| SEC-04 | 機密情報をフロントに渡さない設計規約（トークンは Rust 側で保持し、必要な操作のみ command で公開） | P0 |
| SEC-05 | ファイルパス・SQL 入力に対するバリデーション方針 | P1 |

### 4.8 ドキュメント・DX

| ID | 要件 | 優先度 |
|---|---|:--:|
| DOC-01 | README（OS ごとの前提パッケージ、起動・ビルド手順、モバイル開発の前提） | P0 |
| DOC-02 | アーキテクチャドキュメント（層構成、コマンド追加の手順、型生成の流れ） | P0 |
| DOC-03 | このテンプレートから新規プロジェクトを開始する手順 | P0 |
| DOC-04 | `CLAUDE.md`（AI エージェント向けの規約と禁止事項） | P1 |
| DOC-05 | レシピ集（P2 機能の追加手順、署名証明書の準備手順、ストア申請の前提） | P1 |
| DOC-06 | LICENSE（MIT-0）、CONTRIBUTING、Issue / PR テンプレート | P1 |
| DOC-07 | `docs/automation.md` — Hooks とコード生成の運用、および強制の無効化手順 | P1 |

### 4.9 レビュー体制

レビュー観点を明文化し、それを **Copilot・人間・AI エージェントの 3 者が同じ基準として参照する** 構造にする。
観点そのものは §10 に定義し、各ツールの設定ファイルはそこから導出する。
観点を書く作業がそのままレビュー基準の定義になるため、これは実装より前に着手する。

| ID | 要件 | 優先度 |
|---|---|:--:|
| REV-01 | レビュー観点チェックリストを `docs/review-checklist.md` として単一の正本にする（§10 が原案） | P0 |
| REV-02 | `.github/copilot-instructions.md` — リポジトリ全体に効く Copilot 向け指示。技術スタック・禁止事項・レビュー観点を記述する | P0 |
| REV-03 | パス別のレビュー指示（`src-tauri/**`、`src/**`、`.github/workflows/**` で見るべき点が異なるため分割する） | P1 |
| REV-04 | PR への Copilot コードレビュー自動リクエスト（ルールセットで設定）。**マージ条件には含めない** | P1 |
| REV-05 | 自動生成ファイルをレビュー・差分検出の対象から除外する設定（`bindings.ts`、lockfile、`components/ui/` 等） | P1 |
| REV-06 | PR テンプレートに、観点に対応した自己チェック項目を埋め込む | P1 |
| REV-07 | `CLAUDE.md` に同じ観点を反映し、AI エージェントが実装時点で自己チェックできるようにする | P1 |
| REV-08 | レビュー観点の更新フローの明文化（不具合を出したらその観点を追加する、という運用） | P2 |

---

## 5. ディレクトリ構成（案）

```
.
├── .claude/
│   ├── settings.json           # Hooks（TDD 強制。§9.6）
│   └── skills/
├── scripts/                    # 生成・スキャフォールドのスクリプト
├── .github/
│   ├── workflows/              # ci.yml / release-desktop.yml / build-mobile.yml
│   ├── instructions/           # パス別の Copilot レビュー指示
│   ├── copilot-instructions.md # リポジトリ全体の Copilot 向け指示
│   ├── ISSUE_TEMPLATE/
│   └── pull_request_template.md
├── docs/
│   ├── requirements.md         # 本ドキュメント
│   ├── setup.md                # 環境構築
│   ├── architecture.md         # 層構成とコマンド追加手順
│   ├── testing.md              # TDD の進め方とテスト規約
│   ├── automation.md           # Hooks とコード生成の運用・無効化手順
│   ├── review-checklist.md     # レビュー観点の正本
│   └── recipes/                # 後付け機能の手順書
├── src/                    # フロントエンド (React)
│   ├── app/                # エントリ、Provider、ルータ設定
│   ├── routes/             # TanStack Router のルート定義
│   ├── components/ui/      # shadcn/ui（生成物）
│   ├── components/         # 共通コンポーネント
│   ├── features/           # 機能単位のまとまり
│   ├── lib/
│   │   ├── bindings.ts     # tauri-specta 生成物（手編集禁止）
│   │   ├── api/            # bindings のラッパ。テストではこの層を差し替える
│   │   └── platform.ts     # デスクトップ／モバイル判定
│   ├── hooks/
│   ├── stores/             # Zustand（UI 状態のみ）
│   ├── styles/
│   ├── locales/            # ja / en
│   └── test/               # テストセットアップ、IPC モック、ファクトリ
├── crates/
│   └── core/               # tauri に依存しない層。TDD の主戦場
│       ├── src/
│       │   ├── domain/     # 純粋ロジック
│       │   └── db/         # sqlx 接続・クエリ
│       ├── migrations/     # SQL マイグレーション
│       └── tests/          # 統合テスト（インメモリ SQLite）
├── src-tauri/
│   ├── src/
│   │   ├── main.rs         # デスクトップのエントリ
│   │   ├── lib.rs          # ビルダー本体（モバイルもここを共有）
│   │   ├── commands/       # #[tauri::command] 群。core を呼ぶ薄いアダプタ
│   │   ├── desktop/        # トレイ、メニュー等（cfg(desktop)）
│   │   ├── mobile/         # モバイル固有（cfg(mobile)）
│   │   ├── state.rs
│   │   └── error.rs
│   ├── tests/              # コマンド層の統合テスト（モックアプリ経由）
│   ├── capabilities/       # default.json / desktop.json / mobile.json
│   ├── icons/
│   ├── Cargo.toml
│   └── tauri.conf.json     # + プラットフォーム別の差分設定
├── e2e/
├── Cargo.toml              # ワークスペース定義
├── biome.json
├── lefthook.yml
├── rust-toolchain.toml
├── vite.config.ts
├── CLAUDE.md
├── LICENSE                 # MIT-0
└── README.md
```

---

## 6. 想定されるリスク・注意点

| # | 内容 | 対応方針 |
|---|---|---|
| R-1 | sqlx のコンパイル時 SQL 検証は、ビルド時に DB かオフラインデータを要求する | オフライン用のクエリメタデータをリポジトリにコミットし、CI で最新性を検証する |
| R-2 | SQLite のネイティブライブラリを iOS / Android 向けにクロスコンパイルする際に詰まりやすい | 早期にモバイルビルドを通して構成を確定させる。難航する場合はモバイル側の DB 利用を段階導入とする |
| R-3 | 署名・公証には有償のアカウントと証明書が必要 | 証明書が無くても CI が通るよう、署名ステップは秘密情報の有無で分岐させる |
| R-4 | iOS のビルドは実行環境と証明書管理の要求が重い | 初版では P2 とし、Android を先行させる |
| R-5 | tauri-specta の生成物とソースがずれると実行時に壊れる | CI で再生成し差分が出たら失敗させる |
| R-6 | デスクトップ専用機能が増えるほどモバイルビルドが壊れやすい | CI にモバイルのコンパイル確認を早期から入れる |
| R-7 | フル装備ゆえに不要機能の削除コストが発生する | 機能ごとにファイルを分離し、削除手順を docs に記載する |
| R-8 | Copilot は指示ファイルに書かれた観点しか見ず、大きな差分では取りこぼす | 一次フィルタと位置づけ、人間のレビューを代替させない。PR を小さく保つ運用と対で扱う |
| R-9 | 自動生成ファイルを除外しないと、Copilot のレビューがノイズで埋まる | REV-05 を早期に入れる |
| R-10 | Tauri のコマンドは実アプリを起動しないとテストしづらく、TDD が回らない | ロジックをコマンドから分離する設計制約（§9.3）を規約として強制する |
| R-11 | Rust のビルドが重く、TDD の 1 サイクルが長くなる | ロジックを別クレートに切り出し、テスト対象を小さく保つ（決定済み） |
| R-12 | Hooks は Claude Code 経由の編集にしか効かず、人間が直接書くと素通りする | Git hooks と対で用意し、どちらか一方に依存しない |
| R-13 | 強制が強すぎると無効化されて形骸化する | 一時的な無効化手順を用意し（QA-21）、CI ではテスト失敗のみをブロック理由とする |
| R-14 | 生成物をコミットするとマージコンフリクトが起きやすい | 手で解決せず再生成で解決する手順を文書化する |
| R-15 | Hooks でテストを自動実行すると 1 操作あたりの待ち時間が伸びる | 実行対象を変更クレートに絞る。重いテストは対象から外す |
| R-16 | `open` クレートは `/usr/bin/open` の終了コードしか見ないため、既定ブラウザが URL を握り潰しても成功として扱われ、アプリからは検知できない | デモ画面に既定ブラウザの設定確認を促す注記を表示し、アプリ側での検知は行わない。macOS では `openUrl(url, "Safari")` のように `openWith` でブラウザを明示指定するボタンを Issue #38 で追加した |

---

## 7. 未決事項（次に決めるもの）

Phase 0・Phase 1 に関わるものは決定済み（§2 および §4 に反映）。
残りは Phase 3 以降で判断する。

1. **外部 API 連携の具体像** — OAuth を行うか。行う場合、ディープリンクでのコールバック受けが必須になる（Phase 3）
2. **アップデータの配信先** — GitHub Releases か、自前のストレージか（Phase 4）
3. **署名証明書の準備状況** — Apple Developer Program の加入有無、Windows コード署名証明書の有無（Phase 4）
4. **クラッシュレポートの外部送信** — 収集サービスを入れるか、ローカルログのみに留めるか（Phase 4）
5. **モバイルのリリース範囲** — CI でストア配信まで行うか、成果物生成までとするか（Phase 5）
6. **Hooks の介入の強さ** — テスト不在の実装ファイル作成を「ブロック」するか「警告」に留めるか（Phase 2）。
   規約を確実に守らせるならブロック、試行錯誤の自由度を残すなら警告。**まず警告で運用し、
   守られない実績が出てからブロックに上げる**ことを推奨する

### 決定済みの事項

| 項目 | 決定 |
|---|---|
| ルーティング | TanStack Router |
| 状態管理 | Zustand（UI 状態）+ TanStack Query（Rust 呼び出し） |
| i18n | 日英 2 ロケールを初版から同梱 |
| Rust クレート構成 | ワークスペース分割（`crates/core` を tauri 非依存で切り出す） |
| TDD の適用範囲 | §9.2 の層別区分で確定 |
| カバレッジ | 可視化のみ。閾値は任意で有効化できる形にする |
| Copilot レビュー | 自動リクエストのみ。マージ条件には含めない |
| ライセンス | MIT-0 |

---

## 8. 実装フェーズ案

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| Phase 0 | レビュー観点とテスト方針の確定（REV-01・REV-02、QA-09、`docs/testing.md`） | 観点の正本と Copilot 指示が存在し、以降の PR がその基準で見られる |
| Phase 1 | 基盤 + フロント初期化 + Rust 骨格 + テスト基盤（BASE / FE-01〜05 / RS-01〜06 / SEC-01〜04 / QA-01〜09 / GEN-01・GEN-03） | 3 OS でアプリが起動し型付きコマンドが往復する。かつ Red-Green-Refactor が両言語で回る |
| Phase 2 | CI・レビュー自動化・TDD 強制（CI-01・CI-02、REV-04、QA-10、QA-16〜21、GEN-02） | PR チェックが通り、Copilot レビューが自動で走り、Hooks と Git hooks が機能する |
| Phase 3 | スキャフォールドと土台機能（GEN-04〜06、RS-07〜13、APP-01〜03） | 機能追加が 1 コマンドで Red 状態から始まる。ログ・設定・DB・エラー処理が動作する |
| Phase 4 | デスクトップ機能（APP-04〜08）とリリース（CI-03〜05、CI-08） | 署名済みインストーラが生成され、自動更新が動作する |
| Phase 5 | モバイル対応（FE-07、APP-09、CI-06） | Android で起動し、成果物が CI で生成される |
| Phase 6 | 仕上げ（QA-11〜15、GEN-07〜09、DOC 一式、CI-09〜10、REV-03・REV-05〜08） | ドキュメントが揃い、テンプレートとして利用可能になる |

---

## 9. TDD の進め方

### 9.1 基本サイクル

1. **Red** — 失敗するテストを先に書く。仕様を「振る舞い」として表現する
2. **Green** — テストを通す最小限の実装を書く
3. **Refactor** — テストが緑のまま構造を整える

このサイクルを回せることが Phase 1 の完了条件であり、テスト基盤は最初から用意する。

### 9.2 層ごとの適用方針

TDD が有効に働く層とそうでない層を分ける。全てに機械的には適用しない。
以下の区分を確定とし、レビュー観点（§10.5）もこれを基準とする。

| 層 | 適用 | テスト手段 |
|---|:--:|---|
| Rust: ドメインロジック・純粋関数 | 必須 | ユニットテスト |
| Rust: DB アクセス | 必須 | インメモリ SQLite に対する統合テスト |
| Rust: コマンド層 | 推奨 | モックアプリ経由で入出力を検証 |
| Rust: OS 連携（トレイ・通知・FS） | 対象外 | 薄いラッパーに留め、手動確認とする |
| フロント: hooks・ストア・変換ロジック | 必須 | Vitest |
| フロント: コンポーネント | 推奨 | Testing Library（振る舞い単位。スナップショットは使わない） |
| フロント: 画面全体の結線 | 対象外 | E2E で担保する |

### 9.3 テスト可能性を担保する設計制約

TDD を回すには実装側の構造が前提になる。以下をテンプレートの設計規約として強制する。

- **コマンドを薄く保つ** — `#[tauri::command]` は入力の変換・検証とロジック関数の呼び出しだけを行う。ロジックは `crates/core`（tauri に依存しない別クレート）に置き、それ単体でテストする。クレートを分けることで、この制約を構造として強制すると同時に、テスト時のビルドを軽くする
- **副作用を境界に寄せる** — ファイル I/O・HTTP・時刻取得はトレイト越しに扱い、テストでは差し替える
- **DB 接続は引数で受け取る** — グローバル状態から取得しない。テストごとに独立した接続を渡せるようにする
- **フロントは生成された bindings を直接呼ばない** — 一段ラップし、テストではその層を差し替える。IPC そのもののモックが必要な場合のみ Tauri のモック機構を使う

### 9.4 回転速度の担保

1 サイクルの待ち時間が長いと TDD は破綻するため、以下を初期整備の対象とする。

- フロントのユニットテストはウォッチモードで即時に再実行される
- Rust のテストは変更検知で自動再実行される。ビルドが重いため、テスト対象を小さく保つクレート構成も検討する
- **アプリの起動を伴うテストはサイクルに入れない** — E2E とネイティブ機能の確認は PR 前・CI 側に置く

### 9.5 CI との関係

- PR チェックではユニット・統合テストを全て実行する
- E2E は別ワークフローとし、TDD の速度を犠牲にしない
- **CI は「テストが落ちたこと」だけを理由にブロックする。「テストが無いこと」ではブロックしない** — 誤検知のコストが高いため。テストの不在は手元の介入（§9.6）とレビュー観点（§10.5）で捕まえる

### 9.6 強制の仕組み

規約として書くだけでは守られない。強制を 4 層に分け、
**手元では強く介入し、CI では落ちたテストだけを理由にブロックする** という配分にする。
手元の介入は誤検知しても即座に無視・修正できるが、CI のブロックは誤検知のコストが高いため。

| 層 | 手段 | 介入の強さ |
|---|---|---|
| 実装中（AI エージェント） | Claude Code の Hooks | 強い。テスト不在の実装ファイル作成を止め、編集直後にテストを自動実行する |
| コミット / プッシュ時 | Git hooks（lefthook） | 中。変更範囲のテストを実行し、落ちていれば止める |
| CI | GitHub Actions | テストの失敗のみでブロックする |
| レビュー | 観点チェック（§10.5） | 人間と Copilot が判断する |

Hooks は **Claude Code 経由の編集にしか効かない**。人間がエディタで直接書く場合は素通りするため、
Git hooks と必ず対で用意する。どちらか一方では穴が残る。

| ID | 要件 | 優先度 |
|---|---|:--:|
| QA-16 | Hooks: `crates/core` の実装ファイルを編集した直後に該当クレートのテストを実行し、結果をエージェントに返す | P1 |
| QA-17 | Hooks: 対応するテストが存在しない実装ファイルの新規作成を止める（Red から始めさせる） | P1 |
| QA-18 | Hooks: 応答を終える前に高速テスト一式を実行し、失敗していれば知らせる | P1 |
| QA-19 | Git hooks: プッシュ前に変更範囲のテストを実行する | P1 |
| QA-20 | Hooks の設定をリポジトリにコミットし、テンプレート利用者にも同じ強制がかかるようにする | P1 |
| QA-21 | 強制を一時的に無効化する手順の文書化（**逃げ道の無い強制は回避されるだけ**） | P1 |

---

## 10. レビュー観点（原案）

`docs/review-checklist.md` の原案であり、Copilot 向け指示ファイルもここから導出する。
**Copilot は書かれた観点しか見ない** ため、この一覧の網羅性がレビュー品質を直接決める。

### 10.1 セキュリティ / Tauri 固有

- capabilities に権限を追加していないか。追加している場合、その window・その機能に本当に必要な範囲か
- フロントから受け取った値を、検証せずファイルパスやシェル引数に渡していないか
- 機密情報（トークン・鍵・パスワード）がフロントに返っていないか、ログに出ていないか
- CSP を緩めていないか。`withGlobalTauri` を有効化していないか
- 外部から受け取った URL・パスの検証があるか

### 10.2 Rust

- コマンド経路に `unwrap()` / `expect()` / `panic!` が無いか（プロセスごと落ちる）
- エラーが握り潰されていないか。共通エラー型に変換されてフロントに伝わるか
- ロックを保持したまま `await` していないか
- ブロッキング I/O を非同期コンテキストで直接呼んでいないか
- SQL が文字列連結で組まれていないか。コンパイル時検証を通っているか
- マイグレーションが既存データを壊さないか（追加のみか、破壊的か）
- ロジックがコマンド関数に直接書かれていないか（テスト不能になる。§9.3）

### 10.3 プラットフォーム分岐

- デスクトップ専用 API が共通コードに混入していないか（モバイルビルドが壊れる）
- `#[cfg(desktop)]` / `#[cfg(mobile)]` の対になる実装が揃っているか
- 追加した機能がモバイルで意味を持つか。持たない場合、UI 側でも出し分けているか

### 10.4 フロントエンド

- 生成物（`bindings.ts`）を手編集していないか
- Rust 呼び出しのエラーがハンドリングされ、ユーザーに伝わるか
- ローディング状態が表現されているか（ネイティブ処理は数秒かかりうる）
- デスクトップ幅・モバイル幅の双方で崩れないか。セーフエリアを考慮しているか
- テーマトークンを使っているか（色を直接書いていないか）
- ハードコードされた文言が i18n の対象になっていないか

### 10.5 テスト

- 変更された振る舞いに対応するテストがあるか
- テストが実装の内部構造ではなく振る舞いを検証しているか
- テスト名から「何が保証されているか」が読めるか
- テスト間に実行順依存や共有状態が無いか
- 失敗ケース・境界値が含まれているか

### 10.6 その他

- 依存を追加した理由が妥当か。バンドルサイズとライセンスを確認したか
- 設定値がハードコードされていないか
- ドキュメント（README / architecture）の更新が必要な変更ではないか

### 10.7 Copilot に任せないもの

- 自動生成ファイル（`bindings.ts`、lockfile、`components/ui/` のコピー物）は除外設定を入れ、レビュー対象から外す
- アーキテクチャの妥当性や機能の要否といった判断は、人間のレビューに残す
- Copilot のレビューは人間のレビューを代替しない。**観点の取りこぼしを減らす一次フィルタ** と位置づける

---

## 11. コード生成の運用

手で書く量が増えるほど規約は守られなくなり、テストも書かれなくなる。
これは §9.6 の強制と同じ問題の裏側であり、**手書きの範囲そのものを狭める** ことで対処する。
方針は一つで、正本を一箇所に定め、そこから一方向に生成する。

### 11.1 正本と生成物

| 正本 | 生成物 | 手段 |
|---|---|---|
| Rust のコマンド・イベント定義 | TS の型付きクライアント（`bindings.ts`） | tauri-specta |
| マイグレーション SQL | クエリのコンパイル時検証 | sqlx |
| ルートファイルの配置 | ルート定義 | TanStack Router のプラグイン |
| ロケール JSON | 翻訳キーの型 | 型生成スクリプト |
| プラグインの権限定義 | capabilities の記述候補 | Tauri CLI |
| アプリアイコン 1 枚 | 各プラットフォームのアイコン一式 | Tauri CLI |
| コミット履歴 | CHANGELOG・バージョン | release-please |

原則:

- **生成物は手編集しない。** ヘッダにその旨を明示する
- **生成物はコミットする。** CI で再生成し、差分が出たら失敗させる（ずれたまま動く状態を作らない）
- **生成物はレビュー・lint の対象から外す**（REV-05）
- **生成の向きは常に一方向にし、循環させない**

### 11.2 スキャフォールド

機能を 1 つ足すたびに、以下を人手で並べることになる。ここが最も抜けやすく、
抜けるのはたいていテストである。

- `crates/core` のロジックとそのテスト
- コマンドのアダプタとその統合テスト
- フロントの API ラッパ・hooks とそのテスト
- capabilities への権限追加

**この一式を生成するジェネレータをテンプレートに含める。**
生成される雛形には最初から失敗するテストを含め、**Red の状態から始まる** ようにする。
手書きを減らすと同時に、テストの同時生成を既定にすることで TDD の強制にもなる。

### 11.3 要件

| ID | 要件 | 優先度 |
|---|---|:--:|
| GEN-01 | tauri-specta による TS バインディング生成（RS-05 と同一） | P0 |
| GEN-02 | 全生成物について、再生成後の差分を CI で検出する | P0 |
| GEN-03 | 生成物への「手編集禁止」ヘッダと、lint / レビューの除外設定 | P0 |
| GEN-04 | 機能追加のスキャフォールド（core・コマンド・フロント・テスト一式を Red 状態で生成） | P1 |
| GEN-05 | ロケール JSON からの翻訳キー型生成と、未翻訳キーの検出 | P1 |
| GEN-06 | 生成コマンドを 1 つに束ねる（1 コマンドで全生成物を更新できる状態） | P1 |
| GEN-07 | アイコン一式の生成手順 | P1 |
| GEN-08 | CHANGELOG とバージョンの自動生成（CI-08 と同一） | P1 |
| GEN-09 | スキャフォールドの雛形自体をテストする（生成物が実際にビルド・テストを通ることの確認） | P2 |
