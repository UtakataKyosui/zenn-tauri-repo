# フロントに公開してよい環境変数だけを VITE_ 接頭辞で定義する（BASE-04）。
# 機密情報（API キー・トークン等）はここに置かず、Rust 側（src-tauri）で
# 環境変数または OS キーチェーン（RS-12）経由で保持すること。

# アプリの表示名。src/app で参照するサンプル値。
VITE_APP_NAME=Tauri App Template

# ログレベル（Rust 側）。error/warn/info/debug/trace。RS-07 参照。
RUST_LOG=info
